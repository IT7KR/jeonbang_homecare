/**
 * Admin API Client
 * 관리자 API - 모듈별로 분리된 API 재export
 *
 * 새 코드에서는 직접 모듈을 import하는 것을 권장:
 * import { adminLogin } from "@/lib/api/admin/auth";
 *
 * 기존 호환성을 위해 이 파일에서 모두 재export
 */

export * from "./admin/index";
